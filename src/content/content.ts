// Content script for PDM extension
// Handles communication between web pages and the extension

console.log('PDM Content script loaded');

class PDMContentScript {
  constructor() {
    this.init();
  }

  private init() {
    // Initialize content script
  }
}

new PDMContentScript();

export {};